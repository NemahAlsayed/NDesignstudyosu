- name: Build Hugo site
  run: hugo --minify
  working-directory: Main/myblog   # No space, just Main/myblog

- name: Upload artifact
  uses: actions/upload-pages-artifact@v3
  with:
    path: Main/myblog/public